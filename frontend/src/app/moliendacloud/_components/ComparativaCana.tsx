'use client';
import { PremiumPanel } from '@/components/industrial/PremiumPanel';

export function ComparativaCana() {
  return (
    <PremiumPanel title="COMPARATIVA · CAÑA PESADA VS MOLIENDA" subtitle="Balance de Masa" accent="accent">
      <p className="text-sm py-8 text-center" style={{ color: 'var(--text-muted)' }}>En construcción</p>
    </PremiumPanel>
  );
}
