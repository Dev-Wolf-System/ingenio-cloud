'use client';
import { PremiumPanel } from '@/components/industrial/PremiumPanel';

export function MovimientosHero() {
  return (
    <PremiumPanel title="MOVIMIENTOS · RESUMEN DEL DÍA" subtitle="Logística de Molienda" accent="primary">
      <p className="text-sm py-8 text-center" style={{ color: 'var(--text-muted)' }}>En construcción</p>
    </PremiumPanel>
  );
}
